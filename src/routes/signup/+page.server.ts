export const prerender = false; // Disable static prerendering
export const ssr = true;        // Ensure server-side rendering where needed
export const csr = true;        // Client-side interactivity remains